export const metadata = {
  title: '<Concrete, specific title — no company/product names>',
  description:
    '<One sentence: the problem + the headline result. Mirror this in the data.ts entry.>',
  authors: [{ name: '<Author name>' }],
}

# <Same as metadata.title>

<Hook: open with the tension. The pain a reader recognizes, why it mattered, one paragraph of
context. Not "This post describes…" — start where it hurt.>

## The starting point

<What existed and why its shape was a problem. Bullet the structural issues so the reader feels the
weight before the fix.>

- <problem 1 — a concrete symptom, not an abstraction>
- <problem 2>
- <problem 3>

## The redesign

<The interesting part. One subsection per key decision. Each: what you did, a small genericized
snippet, and — most importantly — *why*, including the alternative you rejected.>

### <Decision 1 — named for the idea, e.g. "Event-driven, not polling">

<Reasoning first, then show it.>

```yaml
# genericized snippet illustrating the core idea (no internal names/hosts/IDs)
```

### <Decision 2>

<Reasoning + snippet.>

### <Decision 3>

<Reasoning + snippet.>

## What it gave back

<Only if the numbers are real. State the assumptions, then show the arithmetic.>

| Metric | Before | After |
| --- | --- | --- |
| <metric> | <x> | <y> (**−Z%**) |
| <metric> | <x> | <y> |

> **<per-unit saving> × <volume> ≈ <weekly figure>** → **~<annual figure>**.

### Why <X> is the floor, not the failure

<Honesty note: the irreducible cost, the thing you didn't fix and why, the trade-off you accepted.
This is where expertise shows.>

## Takeaways

- **<Transferable lesson 1>** — <one line another engineer on a different stack can apply>.
- **<Lesson 2>**
- **<Lesson 3>**
- **<Lesson 4>**

<One-line closer that lands the point of the whole piece.>
